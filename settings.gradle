rootProject.name = "stasbar-app"

include 'common'
include 'backend'

enableFeaturePreview('GRADLE_METADATA')

if (INCLUDE_ANDROID == "true") {
    include 'android', 'android:app', 'android:notifications'
}
